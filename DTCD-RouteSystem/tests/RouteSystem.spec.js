import { RouteSystem } from '../src/RouteSystem';

describe('RouteSystem:getRegistrationMeta()', () => {
  test('should be defined', () => {
    expect(RouteSystem.getRegistrationMeta).toBeDefined();
  });
});
